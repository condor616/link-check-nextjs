services:
  link-check:
    build: .
    image: condor616/link-check:latest
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
      - ./.scan_history:/app/.scan_history
    environment:
      - DATABASE_URL=file:/app/data/db.sqlite
    restart: unless-stopped
